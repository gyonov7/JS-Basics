function multiplicationTable(input) {
    let index = 0;
    let n = Number(input[index]);
    index++;
    let sum = 0;

    for (i = 1; i <= 10; i++) {
        console.log(i);
       // console.log(`${i} * ${n} = ${i*n}`);
    }


}
multiplicationTable([5])